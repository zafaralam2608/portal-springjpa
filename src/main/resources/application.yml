spring:
    datasource:
        url: jdbc:h2:file:C:/workspace/database
        driverClassName: org.h2.Driver
        initialize: false
        name: portal
        password: password
        username: sa
    h2:
        console:
            enabled: true
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate:
            ddl-auto: update
